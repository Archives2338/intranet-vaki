import { FechamesCompletoPipe } from './fechames-completo.pipe';

describe('FechamesCompletoPipe', () => {
  it('create an instance', () => {
    const pipe = new FechamesCompletoPipe();
    expect(pipe).toBeTruthy();
  });
});
